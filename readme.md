# WordCounter - Анализатор частоты слов

## 📋 Описание

Высокопроизводительный консольный инструмент для подсчета частоты слов в текстовых файлах. Программа использует конкурентную обработку данных (горутины) для максимальной производительности на многоядерных системах.

### 🚀 Основные функции
- **Подсчет частоты слов** в текстовых файлах любого размера
- **Вывод топ-10** самых часто встречающихся слов
- **Конкурентная обработка** с настраиваемым количеством воркеров
- **Интерактивный прогресс-бар** с отображением скорости обработки
- **Поддержка Unicode** (кириллица, эмодзи, спецсимволы)
- **Graceful shutdown** - корректная обработка Ctrl+C

### ⚙️ Настраиваемые параметры
| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `-file` | Путь к анализируемому файлу (обязательный) | - |
| `-workers` | Количество параллельных воркеров | Число CPU |
| `-chunk-size` | Размер блока для чтения (в байтах) | 64KB |
| `-progress` | Показывать прогресс-бар | true |
| `-case-sensitive` | Учитывать регистр букв | false |
| `-unicode` | Полная поддержка Unicode (кириллица) | false |

## 🛠 Технические особенности

### Архитектура
- **Конвейерная обработка**: чтение → сплиттинг → воркеры → агрегация
- **Буферизированные каналы** для передачи данных между этапами
- **Атомарные счетчики** для потокобезопасной статистики
- **Контексты** для управления временем жизни горутин
- **Продвинутый сплиттинг** с сохранением целостности слов на границах блоков

### Алгоритмы
- **Два режима парсинга**:
  - `ASCII mode`: быстрый парсинг для латиницы и цифр
  - `Unicode mode`: корректная обработка многобайтовых символов
- **Оптимизированное слияние** частичных результатов
- **Сортировка топ-N** с использованием встроенной сортировки
- **Минимальное выделение памяти** при работе с большими файлами

## 📊 Метрики производительности

Программа собирает и отображает:
- Объем обработанных данных
- Мгновенная скорость (KB/s)
- Средняя скорость (KB/s)
- Время выполнения
- Использование памяти

## 🔧 Установка

```bash
# Клонирование репозитория
git clone https://github.com/Alexteplov/frequency_words
cd frequency_words

## Доступные команды 

| Команда | Описание |
|---------|----------|
| `build` | сборка приложения |
| `run` | запуск с тестовым файлом |
| `runru` | запуск с русским текстом и тестовым файлом tolstoy.txt |
| `test` | запуск тестов |
| `bench` | запуск бенчмарков |
| `clean` | очистка |
| `create-test-file` | создание тестового файла |
| `help` | показать эту справку |